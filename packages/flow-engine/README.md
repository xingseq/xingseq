# @xingseq/flow-engine

L3 能力层 — 星序图执行引擎。

## 定位

将用户编排的「星序图」（DAG 工作流）解析为可执行序列，驱动节点逐步执行，管理分支、并发与状态流转。

## 计划能力

- DAG 拓扑排序与执行调度
- 节点执行器：每种节点类型对应一个 executor
- 状态机：节点 pending → running → done / failed
- 条件分支与循环支持
- 执行上下文传递与变量绑定

## 状态

脚手架占位，核心 API 尚未迁入。
