# @xingseq/agent-runtime

L3 能力层 — Agent 运行时管理。

## 定位

管理多 Agent 实例的注册、生命周期、进程隔离、消息通信。是 Agent 从「单体对话」演进到「多 Agent 协作」的基础设施。

## 计划能力

- Agent 实例注册与发现
- 生命周期管理（启动 / 挂起 / 销毁）
- 邮箱模型：Agent 间异步消息投递
- 守护进程：后台 Agent 常驻与健康检查

## 状态

脚手架占位，核心 API 尚未迁入。
